import 'package:flutter/material.dart';

class TestFeature extends StatelessWidget {
  const TestFeature({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('I am a test feature');
  }
}
